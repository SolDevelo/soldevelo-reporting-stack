slice_name: "Consumption per District"
viz_type: echarts_timeseries_line
uuid: "c1d2e3f4-a5b6-4c7d-8e9f-000000000042"
version: "1.0.0"
dataset_uuid: "a1b2c3d4-e5f6-4a7b-8c9d-000000000010"
description: "Ratio of raw consumption to adjusted consumption per district (sum(consumption)/sum(adjusted_consumption)). A value near 1.0 means raw and adjusted consumption agree; values >1 indicate adjustments reduce reported consumption, <1 indicate adjustments increase it."
certified_by: null
certification_details: null
is_managed_externally: false
cache_timeout: null
params:
  viz_type: echarts_timeseries_line
  x_axis: period_end_date
  time_grain_sqla: P1M
  metrics:
    - expressionType: SQL
      sqlExpression: "CASE WHEN SUM(adjusted_consumption) != 0 THEN SUM(total_consumed_quantity) / SUM(adjusted_consumption) ELSE 0 END"
      label: "Consumption / Adjusted Consumption"
  groupby:
    - zone_name
  adhoc_filters: []
  time_range: "Last 12 months"
  row_limit: 10000
  order_desc: true
  color_scheme: supersetColors
  show_legend: true
  legendType: scroll
  legendOrientation: top
  x_axis_title: Period
  y_axis_title: Ratio
  y_axis_format: ".2f"
  rich_tooltip: true
  tooltipTimeFormat: "%b %Y"
  show_value: false
  markerEnabled: true
  markerSize: 6
  zoomable: true
query_context: null
