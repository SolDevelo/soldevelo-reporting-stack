<clickhouse>
    <profiles>
        <default>
            <!-- Allow Kafka engine (disabled by default in recent ClickHouse) -->
            <allow_experimental_kafka_offsets_storage_in_keeper>0</allow_experimental_kafka_offsets_storage_in_keeper>
        </default>
    </profiles>
</clickhouse>
